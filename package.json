{
  "name": "buger-eats",
  "version": "1.0.0",
  "description": "Projeto de teste front-end para o sistema buger eats",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress open"
  },
  "keywords": [
    "cypress"
  ],
  "author": "Ana Carolina Cerqueira",
  "license": "ISC",
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.34.0",
    "allure": "0.0.0",
    "cypress": "^11.1.0",
    "cypress-file-upload": "^5.0.8",
    "faker": "^5.5.3"
  },
    "cypress:run:loginModule": "npx cypress run --browser chrome --headless --env allure=true --reporter mocha-allure-reporter",
    "report:login:allure": "allure generate allure-results --clean -o allure-report && allure open allure-report"
} 
